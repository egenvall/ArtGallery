import SwiftUI
struct GalleryEmptyStateView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("No results found... Try something else?")
            Spacer()
        }
    }
}
